rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Exige o login (anônimo) do Firebase — fecha o acesso de quem não está no app.
      allow read, write: if request.auth != null;
    }
  }
}
